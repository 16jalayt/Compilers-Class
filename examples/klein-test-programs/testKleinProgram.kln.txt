(*--------------------------------------------------------------------------------------------------*
*                                                                                                   *
* This program is meant for testing purposes only, thus its actual output is meaningless.           *
* It was written to incorporate an array of different token types to make sure our scanner function *
* works properly. It may be added onto or modified as the semester continues.                       *
*                                                                                                   *
* The main function takes in two integers and manipulates the first integer until it matches the    *
* second integer. The functions 'increaseProgram()', 'reduceProgram()', and 'isEqual()' are helper  *
* functions to assist the 'main()' function.                                                        *
*                                                                                                   *
*---------------------------------------------------------------------------------------------------*)

(*------------------------------------- Helper Functions -------------------------------------------*)

function isEqual(x:integer, y:integer) : boolean
	if x = y
		then true
		else false


function increaseProgram( x : integer , y : integer ) : integer
	if isEqual(x, y) = true
		then print( your two integers are now equal)
		     x
		else z=x+1
		     main(z,y)

function reduceProgram(x: integer, y: integer): integer
	if isEqual(x-1,y) = true
		then print( your two integers are now equal)
		     x-1
		else main(x-1, y)

(*------------------------------------- src.Main Function ---------------------------------------------*)

function main(x:integer, y:integer) : integer
	if x<y or x=y
		then increaseProgram(x,y)
		else reduceProgram( x, y )

(*------------------------------------- End of Code -----------------------------------------------*)
	